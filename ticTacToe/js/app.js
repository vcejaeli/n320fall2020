//elements
const statusDiv = document.querySelector('.status');
const blockDivs = document.querySelectorAll('.game-block');

//functionality
let gameIsLive = true;
let xTurn = true;

//unsure on how to go about this..

for (const bloxkDiv of blockDivs  ) {
    console.log(blockDiv);
}